{% extends 'base.html.twig' %}

{% block title %}

    {{ 'title.delete_track'|trans }}

{% endblock %}

{% block body %}
    <h1>
        {{ 'title.delete_track'|trans }}
    </h1>

    {{ 'text.deletion_track'|trans }}

    <br>
    {{ 'index.table'|trans }}{{ ': ' }}{{ partData.INDEKS }}
    <br>

    {{ 'name.table'|trans }}{{ ': ' }}{{ partData.NAZWA }}
    <br>

    {{ form_start(form, {method:'POST', action: url('track_delete', {'trackID': trackID}) }) }}
    {{ form_widget(form) }}
    {#<input type="submit" value="{{ 'action.delete_user'|trans }}" class="btn btn-success" />#}
    {{ form_end(form) }}
    {# ( {'attr': {'onclick': 'return confirm("Are you sure?")'}})#}
{% endblock %}